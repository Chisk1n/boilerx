# {{name}} — local environment template
#
# Copy to `.env` (gitignored) and fill in real values. NEVER commit a real `.env`.

{{#if defaultPort}}
PORT={{defaultPort}}
{{/if}}
NODE_ENV=development
LOG_LEVEL=info
