---
description: Conventions for the {{name}} repository (auto-generated by boilerX)
alwaysApply: true
---

# {{name}}

This is a `{{stack}}` ({{language}}) project generated by boilerX. The full set
of conventions lives in `AGENTS.md`; this file mirrors the must-not-violate
rules for Cursor.

## Mandatory before editing

- Read `AGENTS.md`. It is the source of truth for conventions.
{{#if (eq language "typescript")}}
- TypeScript strict mode. No `any` — prefer `unknown` and narrow.
- ESM imports inside the repo MUST end in `.js`.
{{/if}}
{{#if (eq language "python")}}
- Type hints on every public function. `mypy --strict` MUST pass.
{{/if}}
- `make test` MUST pass before claiming a change works.
- `make lint` MUST be clean before opening a PR.

## Sacred files

- `LICENSE`, `commitlint.config.cjs`, the CI workflow under `.github/workflows/`:
  do not change without explicit user request.
{{#if evolveEnabled}}
- `{{judgeMetricFile}}` and anything under `.judge/`: NEVER modify when running
  as a Worker under `boiler evolve`. The Judge is hash-pinned and drift will
  abort the run.
{{/if}}

## Commits

{{#if conventionalCommits}}Conventional Commits enforced by `commitlint`. Examples:
- `feat(api): add /healthz endpoint`
- `fix(db): handle empty result set`
- `chore: bump dependencies`
{{else}}Plain English, imperative mood, no trailing period.
{{/if}}
